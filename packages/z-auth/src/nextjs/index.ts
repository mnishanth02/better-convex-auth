/**
 * Next.js adapter for Better Convex Auth
 *
 * Provides Next.js-specific implementations including:
 * - createAuth() for setting up auth with hooks and components
 * - Pre-built API route handlers (GET/POST)
 * - Next.js-optimized configuration
 */

export * from "./create-auth";
export * from "./handler";
